# PostgreSQL container settings used by Docker Compose.
POSTGRES_DB=app_db
POSTGRES_USER=app_user
POSTGRES_PASSWORD=replace-with-a-strong-password

# PostgreSQL connection used by the indexer. The Docker Compose default points to the db service.
# For a standalone local run, override it, for example: DB_NAME=index python3 ethsync.py
DB_NAME=postgres://app_user:replace-with-a-strong-password@db:5432/app_db

# PostgREST connection and public read-only API settings.
PGRST_DB_URI=postgres://app_user:replace-with-a-strong-password@db:5432/app_db
PGRST_DB_SCHEMA=public
PGRST_DB_ANON_ROLE=web_anon
PGRST_DB_MAX_ROWS=10000
PGRST_OPENAPI_SERVER_PROXY_URI=http://127.0.0.1:3000

# Ethereum RPC endpoint. The Docker Compose default points to the publicnode service.
# For a standalone local run, override it, for example: ETH_URL=http://127.0.0.1:8545 python3 ethsync.py
ETH_URL=ws://publicnode:8546

# First block used only when both the transaction table and sync state are empty.
START_BLOCK=1

# Number of newest blocks excluded from indexing until they receive confirmations.
CONFIRMATIONS_BLOCK=3

# Delay in seconds between synchronization passes.
PERIOD=5

# Optional log file path. Leave empty to log to stdout.
LOG_FILE=

# Enable storing only transactions related to addresses from ADDRESS_FILTER_FILE.
ADDRESS_FILTER_ENABLED=false

# One Ethereum address per line. Standalone relative paths are resolved from this repository.
ADDRESS_FILTER_FILE=addresses.txt
